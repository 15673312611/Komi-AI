# ChatterMate 免 Docker 纯本地极速运行与数据持久化指南

## 1. 为什么重启电脑后提示「让我们来完成您企业的初始化配置」？

### 💡 核心真相
- **您的管理员账号和数据并没有丢失，数据库也绝对没有被重置！**
- **根本原因**：之前数据库（PostgreSQL 端口 15432）是依赖 Docker Desktop 容器运行的。电脑重启后，因为没有打开 Docker Desktop，**数据库服务处于断开（未启动）状态**。
- **前端表现**：前端在请求后端 `/api/v1/organizations/setup-status` 接口时，由于后端连不上数据库返回了异常，前端误以为“这是一个全新的空系统、尚未配置企业”，于是自动跳转到了初始化配置引导页。

---

## 2. 为什么不能直接简单切换为 MySQL？

很多同学会问：*“我电脑上有现成的 MySQL，为什么不能直接改用 MySQL 呢？”*
核心技术原因如下：
1. **AI 知识库向量检索（RAG）核心依赖**：ChatterMate 智能客服的核心功能是根据知识库文档自动检索回答，这深度依赖数据库层面的 **`pgvector` 向量相似度计算引擎**（支持 1536 维向量高维检索 `<->`），而传统 MySQL 并不具备该原生向量检索能力；
2. **高阶特性绑定**：后端 40+ 个核心业务模块、300 余条 SQL 深度采用了 PostgreSQL 特有的 `RETURNING` 语法、`JSONB` 原生索引查询、以及原生 UUID 机制；如果强行转为 MySQL 会导致 AI 知识库检索彻底瘫痪并引发大量语法异常。

---

## 3. 我们为您落地的终极解决方案：【内置便携式本地 SQL 数据库（零 Docker）】

为了让您彻底摆脱 Docker Desktop 的束缚，我们已经在项目根目录下为您**直接配置好了免安装便携式本地 SQL 数据库（`pgsql/`）**：

### 🌟 带来的极致体验：
- ✅ **彻底告别 Docker Desktop**：不需要安装、也不需要打开任何 Docker 软件；
- ✅ **0 内存损耗**：本地 SQL 引擎仅占用 ~15MB 内存（Docker Desktop 通常占用 2GB~4GB）；
- ✅ **秒级启动**：双击 `start-all.bat` 时，启动脚本会自动在后台拉起本地 SQL 引擎（端口 15432），0.1 秒极速就绪；
- ✅ **数据永久本地持久化**：所有数据直接保存在项目下的 `pgsql/data/` 目录中，电脑关机重启数据永久保存！

---

## 4. 日常使用方式（仅需 1 步）

1. **直接双击运行根目录下的 `start-all.bat`**：
   - 自动拉起本地 SQL 数据库（端口 15432）；
   - 自动编译最新 Go 后端（端口 8001）；
   - 自动启动 Vite 前端（端口 3001）；
2. 浏览器打开 `http://localhost:3001`，秒开秒测，畅享极速体验！

3. **停止服务**：双击根目录下的 **`stop-all.bat`** 即可一键安全停止所有服务并释放端口。
